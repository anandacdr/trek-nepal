# macOS
.DS_Store

# Original camera / stock masters — far too large to deploy.
# Web-ready derivatives live in assets/video and assets/img.
_source/

# Editor + tooling
node_modules/
*.log
.vscode/
.idea/
.vercel

# Secrets — never commit
.env
.env.*
!.env.example
*.token
.cf-token
